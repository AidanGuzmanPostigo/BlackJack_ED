package app;
/**
 * Interfaz que gestiona las partidas.
 */
public interface IPartida {
	/**
	 * Crea las entidades y barajas necesarias para el comienzo de partida y llama a la función que da comienzo a la ronda.
	 */
	void empezarPartida();
}